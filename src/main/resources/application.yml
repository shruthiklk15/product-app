spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/devopss
    username: root
    password: root
    hikari:
      auto-commit: false
      maximum-pool-size: 10
  jpa:
   hibernate:
     ddl-auto: update
   show-sql: true